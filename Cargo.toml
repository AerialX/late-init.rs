[package]
name = "late-init"
version = "0.1.0"
edition = "2018"

categories = ["no-std"]

[dependencies]
const-default = { version = "^0.1.0", git = "https://github.com/AerialX/const-default.rs", optional = true }
